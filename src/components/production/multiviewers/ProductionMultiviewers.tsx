'use client';

import Section from '../../section/Section';

interface ProductionMultiviewersProps {}

const ProductionMultiviewers: React.FC<ProductionMultiviewersProps> = (
  props
) => {
  const {} = props;
  return <Section title="Multiviewers"></Section>;
};

export default ProductionMultiviewers;
